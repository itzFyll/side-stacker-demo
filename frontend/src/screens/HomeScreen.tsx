import React from 'react';

const HomeScreen: React.FC = () => (
  <div>
    <h1>Side-Stacker</h1>
    <p>Welcome! Choose a mode to start playing.</p>
    {/* Add buttons for PvP, PvAI, etc. */}
  </div>
);

export default HomeScreen;